###############################################################################################
#
###############################################################################################
. ./config.sh

for script in $(ls | grep '^[0-9]*_.*.sh'); do
  cat $aa/logo && echo "Executing script '$script'." && sleep 5
  ./$script
done
